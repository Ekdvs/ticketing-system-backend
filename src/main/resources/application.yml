spring:
  application:
    name: ticket-booking-backend

  data:
    mongodb:
      uri: mongodb+srv://stu0752678:rg2a8kWUOtzG3BCd@cluster0.kughe.mongodb.net/ticketdb?retryWrites=true&w=majority
      write-concern: ACKNOWLEDGED

server:
  port: 8080

jwt:
  expiration: 3600000

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

payhere:
  merchant-id: ${PAYHERE_MERCHANT_ID}
  merchant-secret: ${PAYHERE_MERCHANT_SECRET}

logging:
  level:
    org.springframework.data.mongodb: INFO